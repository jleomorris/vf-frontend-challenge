// This file is a central application config
// Here we should store any static strings, feature flags, etc.
// Any sensitive information should not be stored directly here
// but can be added by calling them from an env variable

const AppConfig = {
  global: {
    applicationName: 'application name here',
  },
};

export default AppConfig;
